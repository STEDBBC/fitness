package model;

/**
 * Перечисление ролей пользователей в системе.
 */
public enum Role {
    CLIENT,    // обычный клиент — только просмотр занятий
    TRAINER,   // тренер — может назначать занятия
    ADMIN      // администратор — полный доступ
}

